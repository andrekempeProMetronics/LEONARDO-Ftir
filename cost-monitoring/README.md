# cost-monitoring — LEONARDO-Ftir

Project-scoped cost / accounting view. The main pipeline lives in
`PROM-Portfolio/cost-monitoring/`.

Place billing and cost documentation here as it arrives (payment requests,
invoices, funding annexes, hour logs). Nothing is committed by default that
contains sensitive personnel or financial data — keep such files local unless
explicitly cleared for the repo.
